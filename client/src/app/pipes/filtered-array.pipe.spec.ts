import { FilteredArrayPipe } from './filtered-array.pipe';

describe('FilteredArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
